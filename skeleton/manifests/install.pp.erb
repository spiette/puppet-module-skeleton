# == Class <%= metadata.name %>::intall
# This class is meant to be called from <%= metadata.name %>
# It install requires packages
class <%= metadata.name %>::install {
  include <%= metadata.name %>::params
  package { $<%= metadata.name %>::params::pkgname:
    ensure => present,
  }
}
