# == Class <%= metadata.name %>::service
# This class is meant to be called from <%= metadata.name %>
# It ensure the service is running
class <%= metadata.name %>::service {
  include <%= metadata.name %>::params
  service { $<%= metadata.name %>::params::service:
    ensure => running,
    enable => true,
  }
}
